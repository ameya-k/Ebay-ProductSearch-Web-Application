import { CustomZipDirective } from './custom-zip.directive';

describe('CustomZipDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomZipDirective();
    expect(directive).toBeTruthy();
  });
});
