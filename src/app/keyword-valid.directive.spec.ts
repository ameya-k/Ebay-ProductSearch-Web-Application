import { KeywordValidDirective } from './keyword-valid.directive';

describe('KeywordValidDirective', () => {
  it('should create an instance', () => {
    const directive = new KeywordValidDirective();
    expect(directive).toBeTruthy();
  });
});
