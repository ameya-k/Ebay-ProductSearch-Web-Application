import { CustomKeywordDirective } from './custom-keyword.directive';

describe('CustomKeywordDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomKeywordDirective();
    expect(directive).toBeTruthy();
  });
});
